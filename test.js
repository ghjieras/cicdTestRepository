var assert = require('assert')

describe('Array', () => {
  describe('indexOf()', () => {
   it('test', () => {
     assert.equal(-1, [1,2,3].indexOf(0))
     console.log('CICD HELLO WORLD')
   });
 }); 
});